import { MySquareCusomPipePipe } from './my-square-cusom-pipe.pipe';

describe('MySquareCusomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MySquareCusomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
