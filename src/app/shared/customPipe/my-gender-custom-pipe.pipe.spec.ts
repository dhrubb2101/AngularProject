import { MyGenderCustomPipePipe } from './my-gender-custom-pipe.pipe';

describe('MyGenderCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MyGenderCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
