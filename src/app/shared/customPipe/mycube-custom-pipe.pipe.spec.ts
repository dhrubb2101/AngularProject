import { MycubeCustomPipePipe } from './mycube-custom-pipe.pipe';

describe('MycubeCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MycubeCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
