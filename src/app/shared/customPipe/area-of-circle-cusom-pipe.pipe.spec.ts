import { AreaOfCircleCusomPipePipe } from './area-of-circle-cusom-pipe.pipe';

describe('AreaOfCircleCusomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AreaOfCircleCusomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
